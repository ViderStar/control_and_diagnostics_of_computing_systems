# Digital & Memory Testing Labs — Variant 3

Набор лабораторных работ по дисциплине «Контроль и диагностика вычислительных систем».

- **ЛР №1** – метод активизации одномерного пути (схема на рис. 1.4)
- **ЛР №2** – метод активизации многомерного пути (D-алгоритм)
- **ЛР №3** – генератор тестов на базе LFSR (поиск минимального сида, обеспечивающего 100 % покрытие схемы ЛР1)
- **ЛР №4 (соответствует методическим ЛР №7)** – тестирование ОЗУ: WALKING 0/1 & MATS++ (AF, SAF, CFin, CFid)
- **ЛР №5 (соответствует методическим ЛР №8)** – тестирование ОЗУ маршевыми тестами для PNPSF/ANPSF

## 1. Теоретические материалы

📚 **Подробная теория по каждой лабораторной работе** доступна в папке [`labs_info/`](labs_info/):

- [ЛР №1: Метод активизации одномерного пути](labs_info/lab1_theory.md)
- [ЛР №2: D-алгоритм](labs_info/lab2_theory.md)
- [ЛР №3: LFSR-генераторы](labs_info/lab3_theory.md)
- [ЛР №4: Тестирование ОЗУ (классические неисправности)](labs_info/lab4_theory.md)
- [ЛР №5: Тестирование ОЗУ (кодочувствительные неисправности)](labs_info/lab5_theory.md)

## 2. Установка

```bash
pip install -r requirements.txt
```

## 3. Запуск

```bash
# Логические схемы (ЛР1-2)
python main.py

# LFSR-генератор (ЛР3)
python main.py --suite lfsr

# ОЗУ (ЛР4-5, методические ЛР7-8)
python main.py --suite memory

# Запуск всех лабораторных последовательно
python main.py --suite all
```

## 4. Настройки

Все параметры задаются через переменные окружения и живут в `configs/cfg.py`.

| Переменная | Назначение | Значение по умолчанию |
| --- | --- | --- |
| `DEFAULT_VARIANT` | Номер варианта схемы | `3` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `LAB3_POLY` | Порождающий полином LFSR | `x8⊕x6⊕x5⊕x4⊕1` |
| `LAB4_RAM_BITS` | Реальный размер ОЗУ для оценки сложности | `1_048_576` |
| `LAB4_SIM_BITS` | Кол-во ячеек в моделировании (Walking 0/1) | `256` |
| `LAB4_FAULT_SAMPLES` | Число сценариев на тип неисправности | `32` |
| `LAB5_RAM_BITS` | Размер ОЗУ для оценки сложности | `1_048_576` |
| `LAB5_SIM_BITS` | Кол-во ячеек в моделировании (March PS) | `256` |
| `LAB5_FAULT_SAMPLES` | Число паттернных сценариев | `32` |

> Walking 0/1 — тест сложности `~N²`, поэтому симуляция ограничена `LAB4_SIM_BITS`, а итоговая трудоёмкость пересчитывается на 1 Мбит.

## 5. Результаты (вариант 3)

### ЛР1 — активизация одномерного пути
- 16 тестовых наборов
- 100 % покрытие характеристических неисправностей (входы и ветви)
- Пример теста: `110000` обнаруживает `x1/0`, `x2/0`, `F1/0`, `F4/1`, `F5/1`

### ЛР2 — D-алгоритм
- 4 уникальных теста
- 10 обнаруженных неисправностей (основные входы и ветви)
- Сильно сокращённое тестовое множество по сравнению с ЛР1

### ЛР3 — LFSR
- Полином варианта 3: `x8⊕x6⊕x5⊕x4⊕1`
- Перебор 255 допустимых сидов, длина цикла ≤255
- Автоматически подбирается сид с минимальным числом тактов для 100 % покрытия неисправностей ЛР1
- Выводится список циклов / тестовых наборов, при которых происходит обнаружение

### ЛР4 (методическая №7) — классические неисправности ОЗУ
- Тесты: **Walking 0/1** и **MATS++**
- Модели: AF, SAF, CFin, CFid
- Симуляция на 256 ячейках, ~200 сценариев
- Walking 0/1: покрытие 83.9 % (включая все AF/SAF/CFin)
- MATS++: покрытие 83.9 % (все AF/SAF/CFin), CFid покрывается частично
- Оценка трудоёмкости для 1 Мбит:
  - Walking 0/1 ≈ `1.1e12` операций (`~N²`)
  - MATS++ = `6N` ≈ `6.3e6` операций

### ЛР5 (методическая №8) — кодочувствительные неисправности
- Тесты: **MATS++** и **March PS**
- Модели: `PNPSF K9`, `ANPSF K3`
- March PS даёт 100 % покрытие PNPSF/ANPSF, трудоёмкость `23N` (≈ 24 млн операций для 1 Мбит)
- MATS++ инициирует активные неисправности, но не закрывает PNPSF полностью

## 6. Быстрый обзор схемы (вариант 3)

```
F1 = NAND(x1, x2)
F2 = NOT(x4)
F3 = NOR(F2, x6)
F4 = AND(x3, F3)
F5 = OR(F1, F4)  → выход
```

## 7. Структура проекта

```
├── configs/
│   └── cfg.py
├── dto/
│   ├── __init__.py
│   └── circuit.py
├── helpers/
│   ├── __init__.py
│   ├── cube.py
│   ├── lfsr.py
│   ├── logic.py
│   ├── march.py
│   ├── memory_array.py
│   ├── memory_faults.py
│   ├── memory_runner.py
│   └── memory_tests.py
├── lab1/
│   ├── __init__.py
│   └── single_path.py        # ЛР1
├── lab2/
│   ├── __init__.py
│   └── d_algorithm.py        # ЛР2
├── lab3/
│   ├── __init__.py
│   └── runner.py             # ЛР3 (LFSR)
├── lab4/
│   ├── __init__.py
│   ├── runner.py             # ЛР4 (методическая №7)
│   └── tests.py
├── lab5/
│   ├── __init__.py
│   ├── runner.py             # ЛР5 (методическая №8)
│   └── tests.py
├── pipelines/
│   └── pages/
│       ├── app.py            # Логические схемы
│       ├── lfsr.py           # LFSR пайплайн
│       └── memory.py         # Тесты ОЗУ
├── main.py                   # CLI переключатель
└── requirements.txt
```

## 8. Методика

### ЛР1 – активизация одномерного пути
1. Формирование условий наблюдаемости
2. Выбор пути от дефектного полюса к выходу
3. Условия активности каждого элемента на пути
4. Решение системы по входам, симуляция с дефектом

### ЛР2 – D-алгоритм
1. Построение примитивного d-куба неисправности
2. D-проход: распространение `d/D` к выходам
3. Обратный ход: согласование остальных переменных

### ЛР3 – генератор LFSR
1. Полином варианта задаёт размер регистра (`m=8` для варианта 3)
2. Перебираются все сиды `1…2^m-1`, для каждого запускается LFSR длиной `2^m-1`
3. Для каждого цикла формируется тестовый набор (`x1…x6` берутся из младших бит регистра)
4. При попадании вектора из множества тестов ЛР1 фиксируется покрытие соответствующих неисправностей
5. Выбирается сид с минимальным числом тактов до 100 % покрытия (выводится последовательность попаданий)

### ЛР4 – тесты WALKING 0/1 + MATS++
- Реализован базовый массив ОЗУ с адресными, константными и двухполюсными неисправностями
- Каждая модель → отдельный сценарий, оценка покрытия по тестам

### ЛР5 – тесты MATS++ + March PS
- Расширенная модель ячеек с пассивными и активными кодочувствительными дефектами
- March PS покрывает PNPSF K9 и ANPSF K3 на 100 %

## 9. Полезные команды

```bash
# Запуск логических лабораторных в тихом режиме
LOG_LEVEL=WARNING python main.py

# Ускорить симуляцию ОЗУ (например, 128 ячеек)
LAB4_SIM_BITS=128 LAB5_SIM_BITS=128 python main.py --suite memory
```

## 10. Примечания

- Все новые утилиты вынесены в `helpers/` и переиспользуются в ЛР4+ЛР5.
- Документация сведена к одному файлу `README.md`, как просили.
- Для Walking 0/1 ограничен симулируемый объём памяти, но вычисляется реальная трудоёмкость для 1 Мбит.

#### Все лабораторные успешно выполняются:

```python
python main.py
python main.py --suite memory
python main.py --suite lfsr
```